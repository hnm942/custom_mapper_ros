#pragma once

#include <array>
#include <vector>
#include <iostream>
#include <queue>

namespace distance_grid
{
    template <typename T>
    class DistanceGrid
    {
        public:
            DistanceGrid();
            DistanceGrid() 
    };
} // namespace distance_grid
